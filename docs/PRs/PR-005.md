# PR-005 — Base MCP connect skill

|Owner|Wave|Label|Complexity|Est|Depends|Branch|Parallel|
|---|---|---|---|---|---|---|---|
|keyur|1|CORE|3/5|18 min|PR-001|pr-005-base-mcp-skill|C|

## Goal
Connect Eve to the Base MCP server so the agent can act on Base 8453 through the user's own Base Account. Installs the base-mcp skill and proves connectivity before any quote or swap logic lands.

## Scope
- Created: `eve/mcp.json`
- Created: `eve/skills/base-mcp.md`
- Touched: `eve/agent/index.ts` (MCP client wiring)
- Touched: `eve/package.json` (MCP client dependency)

## Steps
1. Install the base-mcp skill per `base/skills` and record usage in `eve/skills/base-mcp.md` (5 min).
2. Add `mcp.json` pointing at `mcp.base.org` and wire the MCP client into the Eve agent (5 min).
3. Configure the `approvalUrl` flow so signing stays in the user's Base Account, keys off-server (4 min).
4. Verify by listing MCP tools from the agent at boot and confirming the server handshake succeeds (4 min).

## Acceptance
- MCP client connects to `mcp.base.org` at agent boot.
- Available Base MCP tools are listed without auth errors.
- Skill doc records the connect and `approvalUrl` flow.
- No swap, quote, or USDC logic in this PR.

## Notes
tbd
