# PR-002 — Photon channel texting

|Owner|Wave|Label|Complexity|Est|Depends|Branch|Parallel|
|---|---|---|---|---|---|---|---|
|shreyash|1|CORE|3/5|18 min|PR-000;PR-001|pr-002-photon-texting|B|

## Goal
Get the first real text loop working: a user message arrives over Photon iMessage transport and Eve answers in the same thread. Proves the surface path end to end before quotes, cards, or money exist.

## Scope
- Created: `eve/agent/channels/photon.ts`
- Touched: `eve/agent/index.ts` (channel registration)
- Touched: `eve/instructions.md` (texting tone pointer)

## Steps
1. Implement the Photon channel in `channels/photon.ts` per the PR-000 contract, wiring inbound text to the Eve agent (6 min).
2. Add echo-plus-help reply logic so any inbound text gets a response in the same thread (4 min).
3. Register the channel in `agent/index.ts` behind env config for Photon credentials (4 min).
4. Verify by sending a test text through the Photon sandbox number and confirming the reply lands in-thread (4 min).

## Acceptance
- Inbound iMessage text via Photon reaches the Eve agent.
- Agent replies in the same thread for any input.
- Credentials come from env, never hardcoded.
- Echo path follows PR-000 contract shapes.

## Notes
tbd
