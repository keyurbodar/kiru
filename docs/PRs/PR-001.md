# PR-001 — Eve init

|Owner|Wave|Label|Complexity|Est|Depends|Branch|Parallel|
|---|---|---|---|---|---|---|---|
|shreyash|1|CORE|2/5|18 min|none|pr-001-eve-init|A|

## Goal
Stand up the Eve TypeScript agent skeleton so later PRs have a place to land. This is the deployable home for channels, tools, and instructions — nothing smart yet, just wired and runnable.

## Scope
- Created: `eve/agent/index.ts`
- Created: `eve/instructions.md`
- Created: `eve/package.json`
- Created: `eve/tsconfig.json`

## Steps
1. Scaffold `eve/` with `package.json`, `tsconfig.json`, and a minimal Eve agent entrypoint in `agent/index.ts` (6 min).
2. Write baseline `instructions.md` describing Kiru as an iMessage tokenized-stocks agent on Base 8453 (4 min).
3. Register a stub tool via `defineTool` so the tool path compiles (4 min).
4. Verify by running the TypeScript build and booting the agent locally, confirming clean startup (4 min).

## Acceptance
- `eve/` builds with no type errors.
- Agent boots locally with baseline instructions loaded.
- Stub tool is registered via `defineTool` and listed at startup.
- No channel, deploy, or memory wiring yet.

## Notes
tbd
