# PR-000 — Eve tool + Sibyl HTTP contracts

|Owner|Wave|Label|Complexity|Est|Depends|Branch|Parallel|
|---|---|---|---|---|---|---|---|
|keyur|1|CORE|1/5|15 min|none|pr-000-contracts|A|

## Goal
Freeze the cross-team seams before anyone builds on them. Eve's money tools and the Sibyl sidecar HTTP contract are agreed once, in writing, so surface and money+memory work never drift apart.

## Scope
- Created: `eve/agent/tools/contracts.ts`
- Created: `sibyl-sidecar/contract.md`
- Touched: `eve/instructions.md` (contract reference links only)

## Steps
1. Define Eve tool signatures `quoteBuy`, `getPosition`, `executeBuy` with zod input/output shapes in `contracts.ts` (4 min).
2. Define Sibyl HTTP routes `recall`, `remember`, `search`, `record` with request/response JSON shapes in `contract.md` (4 min).
3. Pin shared conventions: USDC on Base 8453, `approvalUrl` flow, HOT/WARM/COLD tier mapping (3 min).
4. Verify by typechecking the zod schemas and reviewing `contract.md` shapes for mismatch, fixing on the spot (4 min).

## Acceptance
- `contracts.ts` exports `quoteBuy`, `getPosition`, `executeBuy` signatures with zod shapes.
- `contract.md` documents `recall`/`remember`/`search`/`record` request/response shapes.
- Tier mapping (HOT pending quote/order, WARM positions/basis, COLD fills journal) is stated.
- No implementation code beyond signatures and shapes.

## Notes
tbd
