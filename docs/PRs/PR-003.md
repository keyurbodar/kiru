# PR-003 — Vercel deploy

|Owner|Wave|Label|Complexity|Est|Depends|Branch|Parallel|
|---|---|---|---|---|---|---|---|
|shreyash|1|CORE|2/5|15 min|PR-001|pr-003-vercel-deploy|B|

## Goal
Make the Eve agent reachable from the internet on Vercel with a health signal. Establishes the production runtime every later surface and money PR deploys onto.

## Scope
- Created: `eve/api/health.ts`
- Created: `vercel.json`
- Touched: `eve/agent/index.ts` (export for serverless entry)

## Steps
1. Add a `GET /api/health` route returning status and version JSON (4 min).
2. Add `vercel.json` pinning the Eve serverless entrypoint and runtime config (3 min).
3. Wire the agent entrypoint for serverless import without breaking local boot (4 min).
4. Verify by deploying to Vercel preview and curling `/api/health` for a 200 with the expected JSON (4 min).

## Acceptance
- Preview deploy succeeds with no build errors.
- `GET /api/health` returns 200 with status and version fields.
- Local boot still works alongside the serverless entry.
- No secrets in repo or logs.

## Notes
tbd
